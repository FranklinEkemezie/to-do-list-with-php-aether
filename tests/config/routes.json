{
    "$schema": "../../config/routes.schema.json",
    "/book/:id": {
        "controller": "Book",
        "action": "viewBook"
    },
    "/logout": {
        "controller": "Auth",
        "action": "logout"
    }
}